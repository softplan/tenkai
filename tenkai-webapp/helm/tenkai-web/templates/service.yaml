apiVersion: v1
kind: Service
metadata:
  name: {{ template "tenkai-web.fullname" . }}
  labels:
    app: {{ template "tenkai-web.name" . }}
    chart: {{ template "tenkai-web.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tenkai-web.name" . }}
    release: {{ .Release.Name }}
